pipeline {
    agent any

    stages {
        stage('Deploy to beta') {
            steps {
	        sshagent(credentials: ['jenkins-ssh']) {
		    sh '''
		        ssh root@beta 'cd /var/www/drupal.ka8zrt.com && git pull --ff-only'
		    '''
		}
            }
        }
    }
}
